# NestJS and mediasoup rely on packages that expect a flat-ish tree for decorators
# metadata resolution; keep hoisting predictable across the workspace.
strict-peer-dependencies=false
auto-install-peers=true
